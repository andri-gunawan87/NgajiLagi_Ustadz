<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="NgajiLagi_Ustadz.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:NgajiLagi_Ustadz.View"
    xmlns:views="clr-namespace:NgajiLagi_Ustadz.View.Menu"
    >
    <TabBar>
        <Tab Title="Home"
             Icon="home.png">
            <ShellContent ContentTemplate="{DataTemplate views:Dashboard}" />
        </Tab>
        <Tab Title="Book"
             Icon="book.png">
            <ShellContent ContentTemplate="{DataTemplate views:Dashboard}" />
        </Tab>
        <Tab Title="Notification"
             Icon="notification.png">
            <ShellContent ContentTemplate="{DataTemplate views:Notification}" />
        </Tab>
        <Tab Title="Message"
             Icon="message.png">
            <ShellContent ContentTemplate="{DataTemplate views:Dashboard}" />
        </Tab>
        <Tab Title="Setting"
             Icon="setting.png">
            <ShellContent ContentTemplate="{DataTemplate views:Setting}" />
        </Tab>
    </TabBar>
    
</Shell>
